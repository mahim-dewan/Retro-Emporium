/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "plus.unsplash.com",
      "example.com",
      "img.bbystatic.com",
      "source.unsplash.com",
      "i.ibb.co",
    ],
  },
};

export default nextConfig;
